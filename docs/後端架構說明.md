# CoinVue（幣景）後端架構說明

> 本文檔說明專案的後端技術架構、分層設計和核心概念

---

## 📚 目錄

1. [三層架構設計](#三層架構設計)
2. [專案結構總覽](#專案結構總覽)
3. [資料庫連接機制](#資料庫連接機制)
4. [核心技術說明](#核心技術說明)
5. [API 測試工具](#api-測試工具)

---

## 🏗️ 三層架構設計

### 架構圖

```
📱 前端/Swagger（客戶端）
    ↓ HTTP Request

📝 Controller 層（控制器）
   - 接收 HTTP 請求
   - 格式驗證
   - 呼叫 Service
    ↓

👨‍🍳 Service 層（服務層）
   - 業務邏輯處理
   - 資料驗證
   - 交易管理
   - 呼叫 Repository
    ↓

🗄️ Repository 層（數據訪問層）
   - 定義資料庫操作
   - JPA 自動生成 SQL
   - 資料持久化
    ↓

💾 Database（MySQL）
   - 實際資料儲存
```

---

### 餐廳比喻

| 角色 | 對應層級 | 職責 |
|------|----------|------|
| 客人 | 前端/Swagger | 發出請求 |
| 櫃台服務生 | Controller | 接收訂單、回傳餐點 |
| 廚師 | Service | 處理食材、製作餐點 |
| 倉庫管理員 | Repository | 管理食材庫存 |
| 冰箱/倉庫 | Database | 實際儲存食材 |

---

### 實際範例：用戶註冊流程

```
1. 用戶在 Swagger 點擊「註冊」
   ↓
2. AuthController.register()
   - 接收 { username, email, password }
   - 驗證格式 (@Valid)
   ↓
3. AuthService.register()
   - 檢查 email 是否已存在
   - 加密密碼 (BCrypt)
   - 創建用戶物件
   - 設定預設值 (role: user, status: active)
   ↓
4. UserRepository.save()
   - JPA 自動生成 SQL: INSERT INTO users ...
   - 執行資料庫插入
   ↓
5. MySQL Database
   - 實際儲存到 users 表
   ↓
6. 回傳結果（逆向）
   - Database → Repository → Service → Controller
   - 回傳 { token, user }
```

---

## 📂 專案結構總覽

```
backend/src/main/java/com/crypto/dashboard/
│
├── 📝 controller/                    # Controller 層（接收請求）
│   ├── AuthController.java          # 認證 API（註冊、登入、登出）
│   ├── FavoriteController.java      # 收藏管理 API
│   ├── CoinController.java          # 幣種查詢 API（CoinGecko Proxy）
│   ├── AdminController.java         # 管理員 API（統計、用戶管理）
│   └── AnnouncementController.java  # 公告管理 API
│
├── 👨‍🍳 service/                       # Service 層（業務邏輯）
│   ├── AuthService.java             # 認證業務邏輯
│   │   ├── 註冊驗證（email 重複檢查）
│   │   ├── 密碼加密（BCrypt）
│   │   ├── JWT Token 簽發
│   │   └── 登入驗證
│   │
│   ├── FavoriteService.java         # 收藏業務邏輯
│   │   ├── 防重複收藏
│   │   └── 收藏 CRUD 操作
│   │
│   ├── CoinService.java             # 幣種資料服務
│   │   ├── CoinGecko API 代理
│   │   └── Caffeine Cache（5 分鐘 TTL）
│   │
│   ├── AdminService.java            # 管理員服務
│   │   ├── 統計數據計算
│   │   └── 用戶列表查詢
│   │
│   └── AnnouncementService.java     # 公告服務
│       ├── 公告 CRUD
│       └── 啟用/停用切換
│
├── 🗄️ repository/                    # Repository 層（資料庫操作）
│   ├── UserRepository.java          # 用戶資料操作
│   │   ├── findByEmail()
│   │   ├── existsByEmail()
│   │   └── findByUsername()
│   │
│   ├── AuthTokenRepository.java     # Token 資料操作
│   │   ├── findByToken()
│   │   └── deleteByUserId()
│   │
│   ├── CoinFavoriteRepository.java  # 收藏資料操作
│   │   ├── findByUserId()
│   │   ├── existsByUserAndCoinId()
│   │   └── deleteByUserAndCoinId()
│   │
│   └── AnnouncementRepository.java  # 公告資料操作
│       ├── findByIsActiveTrue()
│       └── findAllByOrderByCreatedAtDesc()
│
├── 📦 entity/                        # Entity 層（資料模型）
│   ├── User.java                    # 用戶實體（對應 users 表）
│   │   ├── id, username, email
│   │   ├── passwordHash
│   │   ├── role (ENUM: admin/user)
│   │   └── status (ENUM: active/suspended/deleted)
│   │
│   ├── AuthToken.java               # Token 實體（對應 auth_tokens 表）
│   │   ├── token, userId
│   │   └── expiresAt
│   │
│   ├── CoinFavorite.java            # 收藏實體（對應 coin_favorites 表）
│   │   ├── userId, coinId
│   │   └── createdAt
│   │
│   └── Announcement.java            # 公告實體（對應 announcements 表）
│       ├── title, content, type
│       ├── isActive, createdBy
│       └── createdAt, updatedAt
│
├── 📋 dto/                           # DTO 層（資料傳輸物件）
│   ├── request/                     # 請求 DTO
│   │   ├── LoginRequest.java       # 登入請求（email, password）
│   │   ├── RegisterRequest.java    # 註冊請求（username, email, password）
│   │   └── AnnouncementRequest.java # 公告請求
│   │
│   └── response/                    # 回應 DTO
│       ├── AuthResponse.java       # 認證回應（token + user）
│       ├── ApiResponse.java        # 統一回應格式
│       ├── AdminStatsResponse.java # 管理員統計
│       └── AnnouncementResponse.java # 公告回應
│
├── 🔧 config/                        # 配置層
│   ├── SecurityConfig.java         # Spring Security 配置
│   │   ├── JWT 驗證
│   │   ├── CORS 設定
│   │   └── 路徑權限設定
│   │
│   ├── OpenApiConfig.java          # Swagger/OpenAPI 配置
│   │   ├── API 文檔資訊
│   │   └── JWT 認證配置
│   │
│   ├── AppConfig.java              # 應用配置
│   │   └── RestTemplate Bean
│   │
│   └── CorsConfig.java             # CORS 跨域配置
│
├── 🔐 filter/                        # 過濾器
│   └── JwtAuthenticationFilter.java # JWT 認證過濾器
│       ├── 解析 Authorization Header
│       ├── 驗證 Token 有效性
│       └── 設定 SecurityContext
│
├── 🛠️ util/                          # 工具類
│   └── JwtUtil.java                # JWT 工具
│       ├── generateToken()        # 生成 Token
│       ├── validateToken()        # 驗證 Token
│       ├── getUserIdFromToken()   # 解析用戶 ID
│       └── getRoleFromToken()     # 解析角色
│
└── ⚠️ exception/                     # 例外處理
    ├── GlobalExceptionHandler.java  # 全域例外處理器
    ├── CryptoDashboardException.java    # 基礎例外類別
    ├── ResourceNotFoundException.java
    ├── ValidationException.java
    ├── InvalidCredentialsException.java
    └── ExternalApiException.java
```

---

## 💾 資料庫連接機制

### JPA vs JDBC

#### ❌ 傳統 JDBC（不用寫！）

```java
// 傳統 JDBC 寫法（很痛苦！）
Connection conn = DriverManager.getConnection("jdbc:mysql://localhost:3306/crypto_dashboard");
PreparedStatement stmt = conn.prepareStatement("INSERT INTO users (username, email) VALUES (?, ?)");
stmt.setString(1, "testuser");
stmt.setString(2, "test@example.com");
stmt.executeUpdate();
stmt.close();
conn.close();
```

**問題**：
- ❌ 需要手寫 SQL
- ❌ 需要手動管理連接（open/close）
- ❌ 需要手動處理結果集
- ❌ 程式碼冗長且容易出錯

---

#### ✅ Spring Data JPA（專案使用）

```java
// JPA 寫法（簡單！）
userRepository.save(user);  // ← 就這麼簡單！
```

**優勢**：
- ✅ 自動生成 SQL
- ✅ 自動管理資料庫連接
- ✅ 自動把 Java 物件轉換成資料庫記錄
- ✅ 自動把查詢結果轉換成 Java 物件
- ✅ 支援方法名稱自動推導查詢

**範例**：
```java
public interface UserRepository extends JpaRepository<User, Long> {

    // 方法名稱自動推導 SQL
    Optional<User> findByEmail(String email);
    // → 自動生成: SELECT * FROM users WHERE email = ?

    boolean existsByEmail(String email);
    // → 自動生成: SELECT COUNT(*) > 0 FROM users WHERE email = ?

    List<User> findByRole(User.Role role);
    // → 自動生成: SELECT * FROM users WHERE role = ?
}
```

---

### 技術堆疊

```
你的程式碼（Java）
    ↓
Spring Data JPA
    ↓ 自動轉換
Hibernate（JPA 實作）
    ↓ 自動生成 SQL
HikariCP（連接池）← 管理資料庫連接！
    ↓ 執行 SQL
JDBC（底層 API）
    ↓
MySQL Driver
    ↓
MySQL Database
```

---

### 🏊 連接池（HikariCP）

#### 什麼是連接池？

**問題**：每次資料庫操作都建立新連接很慢！

```
每次呼叫 API:
建立連接 (100ms) → 執行查詢 (10ms) → 關閉連接 (50ms)
總共 160ms，其中只有 10ms 在做正事！
```

**解決方案**：連接池（Connection Pool）

```
啟動時建立 10 個連接並保持開啟
每次需要時:
從池子借一個連接 (1ms) → 執行查詢 (10ms) → 歸還連接 (1ms)
總共 12ms，快了 13 倍！
```

---

#### HikariCP 配置

**專案已經自動使用 HikariCP！**

查看 `application.yml`:

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/crypto_dashboard
    username: root
    password: soso0301
    driver-class-name: com.mysql.cj.jdbc.Driver

    # HikariCP 配置（自動啟用）
    hikari:
      maximum-pool-size: 10      # 最大連接數：10
      minimum-idle: 5            # 最小閒置連接：5
      connection-timeout: 5000   # 連接超時：5 秒
      idle-timeout: 600000       # 閒置超時：10 分鐘
      max-lifetime: 1800000      # 連接最大存活時間：30 分鐘
```

**運作原理**：

```
應用啟動時:
┌─────────────────────┐
│  HikariCP 連接池    │
│  ┌───┐ ┌───┐ ┌───┐ │
│  │ 1 │ │ 2 │ │ 3 │ │  保持 10 個連接隨時待命
│  └───┘ └───┘ └───┘ │
│  ┌───┐ ┌───┐ ┌───┐ │
│  │ 4 │ │ 5 │ │ 6 │ │
│  └───┘ └───┘ └───┘ │
└─────────────────────┘

當 Service 需要資料庫操作:
1. 從池子借一個連接
2. 執行 SQL
3. 歸還連接到池子
```

**日誌確認**（後端啟動時）：

```
2025-12-11T00:00:02.672+08:00  INFO --- HikariPool-1 - Starting...
2025-12-11T00:00:02.789+08:00  INFO --- HikariPool-1 - Added connection
2025-12-11T00:00:02.790+08:00  INFO --- HikariPool-1 - Start completed.
```

---

## 🔧 核心技術說明

### 1. Spring Boot 3.2.0

**功能**：
- 自動配置（Auto Configuration）
- 內嵌 Tomcat 伺服器
- 簡化依賴管理

### 2. Spring Data JPA + Hibernate

**功能**：
- ORM（物件關聯映射）
- 自動生成 SQL
- 方法名稱推導查詢

### 3. Spring Security + JWT

**功能**：
- JWT Token 認證
- 密碼加密（BCrypt）
- 權限控制（@PreAuthorize）
- CSRF 防護

### 4. HikariCP

**功能**：
- 資料庫連接池
- 高效能連接管理
- 自動回收閒置連接

### 5. Caffeine Cache

**功能**：
- 本地快取（CoinGecko API）
- TTL：5 分鐘自動過期
- 容量：最大 1000 項

### 6. SpringDoc OpenAPI (Swagger)

**功能**：
- 自動生成 API 文檔
- 互動式 API 測試介面
- JWT 認證整合

---

## 🧪 API 測試工具

### Swagger UI

**訪問路徑**：
```
http://localhost:8080/swagger-ui/index.html
```

**功能**：
- 📚 自動生成 API 文檔
- 🧪 互動式 API 測試
- 🔐 JWT Token 認證支援
- 📋 請求/回應範例

**使用流程**：
1. 註冊/登入取得 Token
2. 點擊右上角 🔒 Authorize 設定 Token
3. 測試任意 API
4. 查看回應結果

---

## 📊 架構優勢

### ✅ 分層架構的好處

1. **職責分明**
   - Controller 只管接收和回傳
   - Service 只管業務邏輯
   - Repository 只管資料庫

2. **容易維護**
   - 要改業務邏輯？只改 Service
   - 要改 API 路徑？只改 Controller
   - 要改資料庫？只改 Repository

3. **可重用**
   - 同一個 Service 可以被多個 Controller 使用
   - 同一個 Repository 可以被多個 Service 使用

4. **容易測試**
   - 可以單獨測試每一層
   - 不用啟動整個系統

5. **效能優化**
   - JPA 自動批次操作
   - HikariCP 連接池
   - Caffeine 快取機制

---

## 🔗 相關文件

- [專案記憶（CLAUDE.md）](../CLAUDE.md)
- [資料庫 Schema（schema_v3.sql）](../database/schema_v3.sql)
- [API 文檔（Swagger UI）](http://localhost:8080/swagger-ui/index.html)
- [完整文檔導覽](./README.md)

---

*最後更新：2025-12-11*
*作者：Claude Code*
